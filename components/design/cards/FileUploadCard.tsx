import React from 'react';
import { DesignCard, DesignCardProps } from './DesignCard';

/**
 * ファイルアップロードのカードコンポーネント
 */
export function FileUploadCard(props: DesignCardProps) {
  return (
    <DesignCard {...props} />
  );
}

